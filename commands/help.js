const Discord = require('discord.js'); // Import Discord

module.exports.run = async (bot, message, args, prefix) => {
    message.channel.send("Help instructions is under constuction.");
}

module.exports.config = {
    name: "help",
    aliases: []
}